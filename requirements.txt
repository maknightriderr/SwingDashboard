# Pinned to avoid a numpy 2.x ABI break (numpy 2.x segfaults with packages
# still built against numpy 1.x). Do not unpin numpy without also checking
# scipy and pandas compatibility.
streamlit>=1.40
numpy>=1.26,<2.0
scipy>=1.11,<1.14
pandas>=2.0,<2.3
yfinance>=0.2.50
requests
plotly
streamlit-cookies-controller
streamlit-autorefresh
psycopg2-binary
smartapi-python
pyotp
logzero
websocket-client
